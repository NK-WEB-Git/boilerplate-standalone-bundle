parameters:
    number: 995
      
framework:
    secret: "Three can keep a secret, if two of them are dead."
    router:
        resource: '%kernel.root_dir%/config/routing.yml'
        strict_requirements: ~
    test: ~    
    